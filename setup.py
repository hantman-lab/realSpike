from setuptools import setup, find_packages

setup(
    name="realSpike",                    # Replace with your project name
    version="0.0.1",
    description="flexible neural manipulation using improv",
    author="Caitlin Lewis, Reagan Bullins",
    packages=find_packages(),
    install_requires=[],
    python_requires="~=3.11",
)